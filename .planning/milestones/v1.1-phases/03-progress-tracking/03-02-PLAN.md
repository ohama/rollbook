---
phase: 03-progress-tracking
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/Components/WorkoutList.fs
  - src/Components/MonthlyStats.fs
autonomous: true

must_haves:
  truths:
    - "User sees workouts in list format, sorted by date (most recent first)"
    - "Empty state message shows when no workouts"
    - "User sees monthly workout count and percentage"
    - "Statistics update when month changes"
  artifacts:
    - path: "src/Components/WorkoutList.fs"
      provides: "List view of workout records"
      exports: ["WorkoutListView"]
      min_lines: 40
    - path: "src/Components/MonthlyStats.fs"
      provides: "Monthly statistics display"
      exports: ["MonthlyStatsView"]
      min_lines: 50
  key_links:
    - from: "src/Components/WorkoutList.fs"
      to: "WorkoutRecord array"
      via: "prop parameter"
      pattern: "workouts: WorkoutRecord array"
    - from: "src/Components/MonthlyStats.fs"
      to: "DateHelpers.getDaysInMonth"
      via: "import and call for percentage calculation"
      pattern: "DateHelpers\\.getDaysInMonth"
---

<objective>
Build list view and statistics components for workout history

Purpose: Provide alternative view format (list) and monthly statistics for users to track progress
Output: WorkoutList component showing chronological workout records, MonthlyStats component displaying count and percentage
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-progress-tracking/03-RESEARCH.md
@src/Supabase/Workouts.fs
@src/Pages/Dashboard.fs
@src/Utils/DateHelpers.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build WorkoutList component</name>
  <files>src/Components/WorkoutList.fs</files>
  <action>
Create src/Components/WorkoutList.fs with WorkoutListView component displaying workout records in list format.

Component structure:
1. **WorkoutListView** component with prop:
   - workouts: WorkoutRecord array

2. Sort workouts by date descending (most recent first):
   - Use `Array.sortByDescending (fun w -> w.workout_date)`

3. Render structure:
   - Container: space-y-2
   - If empty (workouts.Length = 0):
     * Show empty state: "운동 기록이 없습니다" (centered, gray text, py-8)
   - Else map over sorted workouts:
     * Each item as card (bg-white rounded-lg p-4 shadow-sm)
     * Flex layout with gap-3
     * Left side: 💪 emoji (text-2xl)
     * Center: workout_date text (text-gray-800 font-medium)
     * Right side: placeholder for future edit/delete buttons (Phase 3+ WORK-02)

4. Use Html.div with prop.key for each workout (workout.id for React key)

5. Import Supabase.Types for WorkoutRecord type

Add module to App.fsproj after Calendar.fs.
  </action>
  <verify>
dotnet build succeeds.

Check WorkoutList.fs:
- Imports WorkoutRecord type
- WorkoutListView component exported
- Uses Array.sortByDescending for date ordering
- Empty state handled with Korean message
- Each workout has prop.key (workout.id)
- Card layout with emoji and date
  </verify>
  <done>
WorkoutList.fs exists, WorkoutListView component renders sorted workout list with empty state, compiles successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build MonthlyStats component</name>
  <files>src/Components/MonthlyStats.fs</files>
  <action>
Create src/Components/MonthlyStats.fs with MonthlyStatsView component displaying monthly workout statistics.

Component structure:
1. **MonthlyStatsView** component with props:
   - workouts: WorkoutRecord array
   - year: int
   - month: int

2. Calculate statistics:
   - totalWorkouts = workouts.Length
   - daysInMonth = getDaysInMonth year month (from DateHelpers)
   - workoutPercentage = (float totalWorkouts / float daysInMonth) * 100.0
   - Handle division by zero (if daysInMonth = 0, percentage = 0.0)

3. Render structure:
   - Container: bg-white rounded-lg p-6 shadow-sm
   - Title: formatMonthYear year month (text-lg font-semibold text-gray-800 mb-4)
   - Grid layout (grid grid-cols-2 gap-4):
     * Left card: Total workouts
       - Number: totalWorkouts (text-3xl font-bold text-indigo-600)
       - Label: "운동 횟수" (text-sm text-gray-600)
     * Right card: Percentage
       - Number: sprintf "%.0f%%" workoutPercentage (text-3xl font-bold text-green-600)
       - Label: "달성률" (text-sm text-gray-600)
   - Each card: text-center

4. Import DateHelpers (formatMonthYear, getDaysInMonth) and Supabase.Types (WorkoutRecord)

Add module to App.fsproj after WorkoutList.fs.
  </action>
  <verify>
dotnet build succeeds.

Check MonthlyStats.fs:
- Imports DateHelpers functions
- MonthlyStatsView component exported
- Calculates totalWorkouts (array length)
- Calculates percentage using getDaysInMonth
- Handles division by zero
- Grid layout with 2 columns
- Korean labels (운동 횟수, 달성률)
- Color coding (indigo for count, green for percentage)
  </verify>
  <done>
MonthlyStats.fs exists, MonthlyStatsView component displays monthly statistics (count and percentage), compiles successfully.
  </done>
</task>

</tasks>

<verification>
After completing both tasks:

1. **Build check:**
   ```bash
   dotnet build
   ```
   Must succeed with no errors.

2. **Code review:**
   - WorkoutList.fs sorts workouts descending by date
   - WorkoutList.fs shows empty state message
   - MonthlyStats.fs calculates percentage correctly
   - MonthlyStats.fs uses DateHelpers.getDaysInMonth
   - Both components use Korean text
   - Both components match existing Feliz patterns

3. **File structure:**
   ```
   src/
   ├── Components/
   │   ├── Calendar.fs
   │   ├── WorkoutList.fs (NEW)
   │   └── MonthlyStats.fs (NEW)
   └── App.fsproj (UPDATED)
   ```
</verification>

<success_criteria>
Plan complete when:
- [x] WorkoutList.fs exists with WorkoutListView component
- [x] MonthlyStats.fs exists with MonthlyStatsView component
- [x] Both files added to App.fsproj
- [x] dotnet build succeeds
- [x] WorkoutList sorts by date descending
- [x] WorkoutList shows empty state
- [x] MonthlyStats calculates count and percentage
- [x] MonthlyStats uses DateHelpers for days-in-month
- [x] Korean UI text throughout
</success_criteria>

<output>
After completion, create `.planning/phases/03-progress-tracking/03-02-SUMMARY.md`
</output>
