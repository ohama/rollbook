---
phase: 07-cloud-deployment
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: []
autonomous: false
user_setup: []

must_haves:
  truths:
    - "User can sign up for new account in production"
    - "User receives verification email (Gmail SMTP working)"
    - "User can verify email and log in"
    - "User can log workout in production"
    - "User can upload photo in production (storage working)"
    - "User can view their own workouts/photos"
    - "User CANNOT see other users' data (RLS enforced)"
    - "PWA installable on mobile devices"
    - "Offline functionality works (service worker active)"
  artifacts:
    - path: "Production database"
      provides: "Real user data in Supabase Cloud"
      validates: "RLS policies working in production context"
    - path: "Production storage"
      provides: "User-uploaded photos in workout-photos bucket"
      validates: "Storage RLS policies prevent cross-user access"
  key_links:
    - from: "Production app"
      to: "Supabase Cloud Auth"
      via: "Email verification flow"
      pattern: "Signup → Email sent → Verify link → Login succeeds"
    - from: "Production app"
      to: "Supabase Cloud Storage"
      via: "Photo upload with signed URLs"
      pattern: "Upload photo → Storage returns URL → Gallery displays photo"
---

<objective>
Verify complete production functionality via end-to-end user testing.

Purpose: Ensure all features work correctly with Supabase Cloud backend, RLS policies enforced, and offline capabilities active.
Output: Confidence that production deployment is fully operational and secure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cloud-deployment/07-RESEARCH.md
@.planning/phases/07-cloud-deployment/07-01-SUMMARY.md
@.planning/phases/07-cloud-deployment/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete production deployment: Vercel frontend + Supabase Cloud backend.

Deployed to: https://rollbook-xxx.vercel.app (from Plan 07-02)
Backend: Supabase Cloud project (from Plan 07-01)
  </what-built>

  <how-to-verify>
**CRITICAL:** Test with two separate user accounts to verify RLS isolation.

**Phase 1: Authentication Flow (User A)**

1. Open production URL in browser: https://rollbook-xxx.vercel.app
2. Click "회원가입" (Sign up)
3. Enter email (use real email you can access): userA@example.com
4. Enter password: strong-password-123
5. Click submit
6. Expected: "인증 이메일을 확인하세요" message appears
7. Check email inbox for verification email
8. Expected: Email from noreply@mail.app.supabase.io with "Confirm your email" link
9. Click verification link in email
10. Expected: Redirects to app with "이메일이 인증되었습니다" message
11. Log in with same email/password
12. Expected: Redirects to dashboard showing "환영합니다" message

**Phase 2: Core Functionality (User A)**

13. On dashboard, click "오늘 운동했다" button (💪 emoji)
14. Expected: Button changes to ⭕, workout recorded
15. Navigate to "내 기록" tab
16. Expected: Calendar view shows today's date marked as workout day
17. Expected: Monthly stats show "1/30" or similar count
18. Switch to list view
19. Expected: Today's workout appears in list with formatted date

**Phase 3: Photo Upload (User A)**

20. Return to Home tab
21. Click "사진 업로드" or camera icon
22. Select test image from device
23. Expected: Progress bar appears during upload
24. Expected: Success message, photo appears in gallery
25. Check photo in gallery displays correctly
26. Verify workout automatically created for photo date

**Phase 4: RLS Security Verification (User B)**

27. Log out from User A account
28. Open production URL in incognito/private browser window
29. Sign up with different email: userB@example.com
30. Verify email and log in as User B
31. Navigate to "내 기록" tab
32. Expected: Calendar is EMPTY (User B should NOT see User A's workouts)
33. Navigate to "팀" (Team) tab
34. Expected: Shows User A and User B in member list
35. Expected: Shows monthly workout counts for both users (e.g., User A: 1, User B: 0)
36. Expected: Does NOT show specific workout dates or photos for User A

**Phase 5: PWA Installability**

37. Open production URL on mobile device (iOS or Android)
38. Tap browser menu (Safari: Share button, Chrome: three dots)
39. Expected: "Add to Home Screen" option available
40. Install app to home screen
41. Open app from home screen icon
42. Expected: Launches in standalone mode (no browser chrome)
43. Expected: App icon and name "Rollbook - 운동 기록" visible

**Phase 6: Offline Functionality**

44. While logged in, enable airplane mode on device
45. Navigate between tabs (Home, 내 기록, 팀)
46. Expected: Previously loaded data still visible (service worker caching)
47. Attempt to log new workout while offline
48. Expected: Operation queued (offline indicator may show)
49. Disable airplane mode (go online)
50. Expected: Queued workout syncs automatically
51. Refresh page to verify workout persisted

**Phase 7: Admin Functionality (if admin role assigned)**

52. If you assigned admin role in Plan 06-02 (INSERT INTO user_roles...):
53. Log in with admin account
54. Navigate to "관리" (Admin) tab
55. Expected: Member list shows all users
56. Expected: Delete buttons visible (do NOT actually delete for this test)
57. If admin role NOT assigned: Admin tab should NOT appear (correct behavior)

**Pass Criteria:**
- All authentication steps succeed (signup, email verification, login)
- User A can create workouts and upload photos
- User B cannot see User A's workout dates or photos (only counts in team view)
- PWA installs and runs in standalone mode
- Offline functionality works (cached data visible, operations queue when offline)
- No console errors throughout testing

**Fail Indicators:**
- Email verification link doesn't work → Check Gmail SMTP config in Supabase
- User B sees User A's workout dates → RLS policies not enforced, check migration 07-01
- Photos return 403 errors → Storage RLS misconfigured, check storage bucket policies
- PWA install not available → Check manifest in vite.config.js, verify HTTPS
- Console errors on page load → Check environment variables in Vercel

If any test fails, describe the issue in detail including:
- Which step failed
- Expected vs. actual behavior
- Browser console errors (if any)
- Network tab errors (if any)
  </how-to-verify>

  <resume-signal>
Type "approved" if all verification steps pass, or describe specific issues found.
  </resume-signal>
</task>

</tasks>

<verification>
Complete manual testing checklist above passes all phases:
- Authentication flow works for multiple users
- Core workout logging functional
- Photo upload and storage working
- RLS security verified (User B cannot see User A's private data)
- PWA installable and works offline
- No console or network errors
</verification>

<success_criteria>
- Two distinct users can sign up, verify email, and log in
- Each user can log workouts and upload photos
- RLS isolation confirmed (users cannot access each other's private data)
- Team visibility working (users can see each other's monthly counts)
- PWA installable on mobile devices
- Offline functionality active (service worker caching and queue working)
- Production app stable with no critical errors
</success_criteria>

<output>
After human verification passes, create `.planning/phases/07-cloud-deployment/07-03-SUMMARY.md` using the summary template.
</output>
