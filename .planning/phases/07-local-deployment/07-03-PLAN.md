---
phase: 07-local-deployment
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - launchd/com.rollbook.supabase.plist
  - launchd/com.rollbook.frontend.plist
  - launchd/com.rollbook.tunnel.plist
  - scripts/rollbook-services.sh
autonomous: true

must_haves:
  truths:
    - "Supabase service starts after Docker is ready (health check loop)"
    - "Frontend service starts after Supabase API is healthy"
    - "Tunnel service starts and connects to Cloudflare"
    - "All three services auto-restart on crash (KeepAlive)"
    - "Services can be loaded/unloaded via launchctl"
  artifacts:
    - path: "launchd/com.rollbook.supabase.plist"
      provides: "Supabase launchd service definition"
      contains: "com.rollbook.supabase"
    - path: "launchd/com.rollbook.frontend.plist"
      provides: "Frontend launchd service definition"
      contains: "com.rollbook.frontend"
    - path: "launchd/com.rollbook.tunnel.plist"
      provides: "Tunnel launchd service definition"
      contains: "com.rollbook.tunnel"
    - path: "scripts/rollbook-services.sh"
      provides: "Helper script to install/start/stop/status all services"
      contains: "launchctl"
  key_links:
    - from: "com.rollbook.supabase.plist ProgramArguments"
      to: "Docker daemon"
      via: "health check: while ! docker info"
      pattern: "docker info"
    - from: "com.rollbook.frontend.plist ProgramArguments"
      to: "Supabase API health endpoint"
      via: "health check: while ! curl localhost:54321"
      pattern: "curl.*54321"
    - from: "com.rollbook.tunnel.plist ProgramArguments"
      to: "cloudflared binary"
      via: "absolute path to cloudflared"
      pattern: "/opt/homebrew/bin/cloudflared"
---

<objective>
Create three launchd plist files for automatic service startup on Mac Mini boot, plus a helper script for managing all services together.

Purpose: All services (Supabase Docker, Vite preview frontend, Cloudflare Tunnel) must auto-start on Mac Mini boot and auto-restart on crash. This enables unattended production operation.
Output: Three plist files in `launchd/` directory, a management script, and services installed in `~/Library/LaunchAgents/`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-local-deployment/07-RESEARCH.md
@.planning/phases/07-local-deployment/07-01-SUMMARY.md
@.planning/phases/07-local-deployment/07-02-SUMMARY.md
@howto/deploy-tunnel.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create launchd plist files for all three services</name>
  <files>launchd/com.rollbook.supabase.plist, launchd/com.rollbook.frontend.plist, launchd/com.rollbook.tunnel.plist</files>
  <action>
    Create `launchd/` directory in project root and write three plist files.
    Get the actual values by running:
    - `whoami` for username
    - `pwd` for project directory (should be the rollbook project root)
    - `which cloudflared` for cloudflared absolute path
    - `which npm` or `which node` for Node.js path

    All plist files MUST use absolute paths (research pitfall #1 -- launchd does not expand ~).
    All plist files MUST include PATH in EnvironmentVariables with `/opt/homebrew/bin` (research pitfall #3).

    **1. com.rollbook.supabase.plist** -- Supabase Docker service:
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
        <key>Label</key>
        <string>com.rollbook.supabase</string>
        <key>ProgramArguments</key>
        <array>
            <string>/bin/bash</string>
            <string>-c</string>
            <string>while ! docker info > /dev/null 2>&1; do echo "Waiting for Docker..." && sleep 5; done && cd /Users/USERNAME/vibe-coding/rollbook && npx supabase start</string>
        </array>
        <key>WorkingDirectory</key>
        <string>/Users/USERNAME/vibe-coding/rollbook</string>
        <key>RunAtLoad</key>
        <true/>
        <key>KeepAlive</key>
        <dict>
            <key>SuccessfulExit</key>
            <false/>
        </dict>
        <key>StandardOutPath</key>
        <string>/tmp/rollbook-supabase.log</string>
        <key>StandardErrorPath</key>
        <string>/tmp/rollbook-supabase.error.log</string>
        <key>EnvironmentVariables</key>
        <dict>
            <key>PATH</key>
            <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
        </dict>
    </dict>
    </plist>
    ```
    Key design: Health check loop waits for Docker daemon before starting Supabase (research pitfall #6).
    KeepAlive with SuccessfulExit=false: only restart on crashes (research pitfall #2).

    **2. com.rollbook.frontend.plist** -- Vite preview server:
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
        <key>Label</key>
        <string>com.rollbook.frontend</string>
        <key>ProgramArguments</key>
        <array>
            <string>/bin/bash</string>
            <string>-c</string>
            <string>while ! curl -sf http://localhost:54321/rest/v1/ -H "apikey: dummy" > /dev/null 2>&amp;1; do echo "Waiting for Supabase API..." &amp;&amp; sleep 5; done &amp;&amp; cd /Users/USERNAME/vibe-coding/rollbook &amp;&amp; npx vite preview --host 0.0.0.0</string>
        </array>
        <key>WorkingDirectory</key>
        <string>/Users/USERNAME/vibe-coding/rollbook</string>
        <key>RunAtLoad</key>
        <true/>
        <key>KeepAlive</key>
        <true/>
        <key>StandardOutPath</key>
        <string>/tmp/rollbook-frontend.log</string>
        <key>StandardErrorPath</key>
        <string>/tmp/rollbook-frontend.error.log</string>
        <key>EnvironmentVariables</key>
        <dict>
            <key>PATH</key>
            <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
            <key>NODE_ENV</key>
            <string>production</string>
        </dict>
    </dict>
    </plist>
    ```
    Key design: Health check waits for Supabase API (research pattern #4).
    KeepAlive=true: always keep running (frontend should never intentionally exit).
    host 0.0.0.0: required for cloudflared to connect (research pitfall #5).

    **3. com.rollbook.tunnel.plist** -- Cloudflare Tunnel:
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
        <key>Label</key>
        <string>com.rollbook.tunnel</string>
        <key>ProgramArguments</key>
        <array>
            <string>/opt/homebrew/bin/cloudflared</string>
            <string>tunnel</string>
            <string>run</string>
            <string>rollbook</string>
        </array>
        <key>RunAtLoad</key>
        <true/>
        <key>KeepAlive</key>
        <true/>
        <key>StandardOutPath</key>
        <string>/tmp/rollbook-tunnel.log</string>
        <key>StandardErrorPath</key>
        <string>/tmp/rollbook-tunnel.error.log</string>
        <key>EnvironmentVariables</key>
        <dict>
            <key>PATH</key>
            <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
        </dict>
    </dict>
    </plist>
    ```
    Key design: Uses absolute path to cloudflared binary (research pitfall #3).
    KeepAlive=true: tunnel should always be running.
    No health check needed -- cloudflared handles reconnection internally.

    Replace `USERNAME` with actual output of `whoami` in ALL files.
    Replace `/opt/homebrew/bin/cloudflared` with actual output of `which cloudflared` if different.

    IMPORTANT for plist XML: Use `&amp;` for `&`, `&amp;&amp;` for `&&`, `&gt;` for `>`, `&lt;` for `<` inside plist string values. This is XML encoding. The bash scripts inside ProgramArguments must be XML-escaped.
  </action>
  <verify>
    - `plutil -lint launchd/com.rollbook.supabase.plist` returns OK
    - `plutil -lint launchd/com.rollbook.frontend.plist` returns OK
    - `plutil -lint launchd/com.rollbook.tunnel.plist` returns OK
    - All three files contain absolute paths (no ~ characters)
    - All three files contain PATH with /opt/homebrew/bin
    - grep for USERNAME returns no results (all replaced with actual username)
  </verify>
  <done>
    Three valid plist files created with proper XML escaping, absolute paths, health check loops, KeepAlive settings, and PATH environment variables.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create service management script and install services</name>
  <files>scripts/rollbook-services.sh</files>
  <action>
    1. **Create management script** at `scripts/rollbook-services.sh`:
       ```bash
       #!/bin/bash
       # Rollbook Services Manager
       # Usage: ./scripts/rollbook-services.sh [install|uninstall|start|stop|restart|status|logs]

       SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
       LAUNCH_AGENTS_DIR="$HOME/Library/LaunchAgents"
       SERVICES=("com.rollbook.supabase" "com.rollbook.frontend" "com.rollbook.tunnel")
       PLIST_DIR="$SCRIPT_DIR/launchd"

       install() {
           mkdir -p "$LAUNCH_AGENTS_DIR"
           for svc in "${SERVICES[@]}"; do
               cp "$PLIST_DIR/$svc.plist" "$LAUNCH_AGENTS_DIR/"
               echo "Installed $svc"
           done
           echo "Done. Run '$0 start' to start services."
       }

       uninstall() {
           stop
           for svc in "${SERVICES[@]}"; do
               rm -f "$LAUNCH_AGENTS_DIR/$svc.plist"
               echo "Removed $svc"
           done
       }

       start() {
           for svc in "${SERVICES[@]}"; do
               launchctl load "$LAUNCH_AGENTS_DIR/$svc.plist" 2>/dev/null
               echo "Started $svc"
           done
       }

       stop() {
           for svc in "${SERVICES[@]}"; do
               launchctl unload "$LAUNCH_AGENTS_DIR/$svc.plist" 2>/dev/null
               echo "Stopped $svc"
           done
       }

       restart() {
           stop
           sleep 2
           start
       }

       status() {
           echo "=== Rollbook Services ==="
           for svc in "${SERVICES[@]}"; do
               pid=$(launchctl list "$svc" 2>/dev/null | grep -o '"PID" = [0-9]*' | grep -o '[0-9]*')
               if [ -n "$pid" ]; then
                   echo "  $svc: RUNNING (PID $pid)"
               else
                   exit_code=$(launchctl list "$svc" 2>/dev/null | grep -o '"LastExitStatus" = [0-9]*' | grep -o '[0-9]*')
                   if [ -n "$exit_code" ]; then
                       echo "  $svc: STOPPED (exit code $exit_code)"
                   else
                       echo "  $svc: NOT LOADED"
                   fi
               fi
           done
           echo ""
           echo "=== Port Check ==="
           echo "  Supabase API (54321): $(curl -sf http://localhost:54321/rest/v1/ -H 'apikey: dummy' > /dev/null 2>&1 && echo 'UP' || echo 'DOWN')"
           echo "  Frontend (4173):      $(curl -sf http://localhost:4173 > /dev/null 2>&1 && echo 'UP' || echo 'DOWN')"
           echo "  Tunnel:               $(launchctl list com.rollbook.tunnel 2>/dev/null | grep -q 'PID' && echo 'CONNECTED' || echo 'DOWN')"
       }

       logs() {
           echo "=== Recent logs ==="
           for svc in "${SERVICES[@]}"; do
               short_name="${svc#com.rollbook.}"
               echo "--- $short_name ---"
               tail -5 "/tmp/rollbook-$short_name.log" 2>/dev/null || echo "  No logs yet"
               echo ""
           done
       }

       case "${1:-status}" in
           install)   install ;;
           uninstall) uninstall ;;
           start)     start ;;
           stop)      stop ;;
           restart)   restart ;;
           status)    status ;;
           logs)      logs ;;
           *)         echo "Usage: $0 [install|uninstall|start|stop|restart|status|logs]" ;;
       esac
       ```

    2. **Make script executable**:
       ```bash
       chmod +x scripts/rollbook-services.sh
       ```

    3. **Install services** (copy plists to LaunchAgents):
       ```bash
       ./scripts/rollbook-services.sh install
       ```

    4. **Verify installation**:
       ```bash
       ls ~/Library/LaunchAgents/com.rollbook.*.plist
       ```

    NOTE: Do NOT start the services yet (that happens in Plan 04 during E2E verification).
    The install step only copies plist files -- it does not load/start them.
  </action>
  <verify>
    - `scripts/rollbook-services.sh` exists and is executable
    - `ls ~/Library/LaunchAgents/com.rollbook.*.plist` shows 3 files
    - `./scripts/rollbook-services.sh status` runs without errors (services show NOT LOADED)
    - `plutil -lint ~/Library/LaunchAgents/com.rollbook.supabase.plist` returns OK
  </verify>
  <done>
    Management script created with install/start/stop/restart/status/logs commands. Three plist files installed in ~/Library/LaunchAgents/. Services are installed but NOT started (ready for Plan 04 verification).
  </done>
</task>

</tasks>

<verification>
- Three plist files in launchd/ directory, all passing `plutil -lint`
- All plists use absolute paths, include PATH env var with /opt/homebrew/bin
- Health check ordering: Docker -> Supabase -> Frontend
- Management script provides install/start/stop/restart/status/logs
- Plist files copied to ~/Library/LaunchAgents/
</verification>

<success_criteria>
- Three valid plist files with proper health check ordering
- Supabase waits for Docker, Frontend waits for Supabase API
- Tunnel uses absolute cloudflared path
- Management script works for all operations
- Services installed in ~/Library/LaunchAgents/ ready to start
</success_criteria>

<output>
After completion, create `.planning/phases/07-local-deployment/07-03-SUMMARY.md`
</output>
