---
phase: 04-team-features
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - src/Pages/Dashboard.fs
autonomous: true

must_haves:
  truths:
    - "Dashboard has three tabs: Home, Progress (내 기록), Team (팀)"
    - "Team tab navigates to TeamView page"
    - "Tab navigation pattern consistent with existing Home/Progress tabs"
    - "User can switch between all three tabs"
  artifacts:
    - path: "src/Pages/Dashboard.fs"
      provides: "Dashboard with Team tab integration"
      contains: ["Team", "TeamViewPage", "TabMode"]
  key_links:
    - from: "src/Pages/Dashboard.fs"
      to: "Pages.TeamView.TeamViewPage"
      via: "import and render"
      pattern: "TeamViewPage"
    - from: "src/Pages/Dashboard.fs"
      to: "TabMode"
      via: "discriminated union case"
      pattern: "| Team"
---

<objective>
Integrate Team tab into Dashboard navigation alongside existing Home and Progress tabs

Purpose: Enable users to access team stats from the main dashboard
Output: Updated Dashboard.fs with three-tab navigation including Team tab
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-team-features/04-03-SUMMARY.md
@src/Pages/Dashboard.fs
@src/Pages/TeamView.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Team tab to Dashboard</name>
  <files>src/Pages/Dashboard.fs</files>
  <action>
Update src/Pages/Dashboard.fs to add Team tab alongside existing Home and Progress tabs.

**Step 1: Update imports**
Add import for TeamView module:
```fsharp
open Pages.TeamView
```
(Add after `open Pages.ProgressView`)

**Step 2: Update TabMode discriminated union**
Change from:
```fsharp
type TabMode = Home | Progress
```
To:
```fsharp
type TabMode = Home | Progress | Team
```

**Step 3: Add Team tab button**
In the tab navigation section (after "내 기록" button), add:
```fsharp
Html.button [
    prop.onClick (fun _ -> setActiveTab Team)
    prop.className (
        "px-6 py-3 rounded-lg font-medium transition-colors " +
        if activeTab = Team then
            "bg-indigo-600 text-white"
        else
            "bg-gray-200 text-gray-700 hover:bg-gray-300"
    )
    prop.text "팀"
]
```

**Step 4: Add Team case to content match**
In the `match activeTab with` section, add:
```fsharp
| Team ->
    TeamViewPage user.id
```
(After the `| Progress ->` case)

**Layout notes:**
- Tab buttons remain in a row (flex gap-2)
- Three buttons: "홈", "내 기록", "팀"
- Team tab passes user.id for current user highlighting in TeamView
- Match existing button styling exactly
  </action>
  <verify>
```bash
dotnet build /home/shoh/vibe-coding/rollbook
```
Build succeeds.

Check TabMode has three cases:
```bash
grep -n "TabMode" /home/shoh/vibe-coding/rollbook/src/Pages/Dashboard.fs | head -3
```
Should show Home | Progress | Team.

Check Team tab button exists:
```bash
grep -n '"팀"' /home/shoh/vibe-coding/rollbook/src/Pages/Dashboard.fs
```
Should show button text.

Check TeamViewPage integration:
```bash
grep -n "TeamViewPage" /home/shoh/vibe-coding/rollbook/src/Pages/Dashboard.fs
```
Should show import and usage.
  </verify>
  <done>
Dashboard.fs updated with Team tab, TabMode extended, TeamViewPage integrated, compiles successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify tab navigation flow</name>
  <files>src/Pages/Dashboard.fs</files>
  <action>
Review the complete Dashboard.fs to ensure:

1. **Import order is correct:**
   - Pages.ProgressView before Pages.TeamView

2. **Tab buttons are in logical order:**
   - "홈" (Home)
   - "내 기록" (Progress)
   - "팀" (Team)

3. **Match cases are exhaustive:**
   - Home case renders workout toggle
   - Progress case renders ProgressViewPage
   - Team case renders TeamViewPage

4. **User ID passed correctly:**
   - ProgressViewPage user.id
   - TeamViewPage user.id

5. **No F# warnings about incomplete pattern match:**
   - All TabMode cases handled

Run dev server to visually verify (optional):
```bash
npm run dev
```
Navigate to dashboard and verify all three tabs are visible.
  </action>
  <verify>
```bash
dotnet build /home/shoh/vibe-coding/rollbook
```
Build succeeds with no warnings about incomplete matches.

Visual check (if dev server running):
- Three tabs visible: 홈, 내 기록, 팀
- Clicking each tab switches content
- Team tab shows TeamViewPage
  </verify>
  <done>
Dashboard tab navigation complete with three tabs (Home, Progress, Team), all cases handled, no compilation warnings.
  </done>
</task>

</tasks>

<verification>
After completing both tasks:

1. **Build check:**
   ```bash
   dotnet build /home/shoh/vibe-coding/rollbook
   ```
   Must succeed with no errors or warnings.

2. **Code structure:**
   - TabMode = Home | Progress | Team
   - Three tab buttons in navigation
   - Three cases in match expression
   - TeamView imported and used

3. **Tab button order:**
   - 홈 (Home)
   - 내 기록 (Progress)
   - 팀 (Team)

4. **Integration verification:**
   - TeamViewPage receives user.id prop
   - Tab styling consistent with existing tabs
   - Button hover/active states work
</verification>

<success_criteria>
Plan complete when:
- [x] TabMode extended with Team case
- [x] Team tab button added with "팀" text
- [x] Team case added to match expression
- [x] TeamViewPage called with user.id
- [x] Import for Pages.TeamView added
- [x] Tab order: 홈, 내 기록, 팀
- [x] Button styling consistent
- [x] dotnet build succeeds with no warnings
- [x] All TabMode cases exhaustively handled
</success_criteria>

<output>
After completion, create `.planning/phases/04-team-features/04-04-SUMMARY.md`
</output>
