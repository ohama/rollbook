---
phase: 04-team-features
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/Pages/Dashboard.fs
autonomous: true

must_haves:
  truths:
    - "Dashboard has Team tab in navigation"
    - "Team tab shows TeamView page"
    - "Tab navigation works between Home, 내 기록, and 팀"
    - "Active tab is visually highlighted"
  artifacts:
    - path: "src/Pages/Dashboard.fs"
      provides: "Dashboard with Team tab integration"
      contains: "TeamViewPage"
      min_lines: 150
  key_links:
    - from: "src/Pages/Dashboard.fs"
      to: "TeamViewPage"
      via: "tab navigation rendering"
      pattern: "TeamViewPage"
    - from: "src/Pages/Dashboard.fs"
      to: "TabMode"
      via: "discriminated union for tab state"
      pattern: "type TabMode.*Team"
---

<objective>
Integrate Team tab into Dashboard navigation

Purpose: Add "팀" tab to Dashboard so users can access team workout visibility
Output: Updated Dashboard.fs with three tabs: 홈, 내 기록, 팀
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-team-features/04-RESEARCH.md
@src/Pages/Dashboard.fs
@src/Pages/TeamView.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Team tab to Dashboard navigation</name>
  <files>src/Pages/Dashboard.fs</files>
  <action>
Update `src/Pages/Dashboard.fs` to add Team tab and integrate TeamViewPage.

**Changes to Dashboard.fs:**

1. **Update TabMode discriminated union:**
```fsharp
/// Tab mode for dashboard navigation
type TabMode = Home | Progress | Team
```

2. **Add import for TeamView:**
```fsharp
open Pages.TeamView
```

3. **Add Team tab button to navigation (after "내 기록"):**
```fsharp
Html.button [
    prop.onClick (fun _ -> setActiveTab Team)
    prop.className (
        "px-6 py-3 rounded-lg font-medium transition-colors " +
        if activeTab = Team then
            "bg-indigo-600 text-white"
        else
            "bg-gray-200 text-gray-700 hover:bg-gray-300"
    )
    prop.text "팀"
]
```

4. **Add Team case to content rendering (after Progress case):**
```fsharp
| Team ->
    TeamViewPage()
```

**Full tab navigation structure:**
```fsharp
// Tab navigation
Html.div [
    prop.className "flex gap-2 mb-6"
    prop.children [
        // Home tab
        Html.button [
            prop.onClick (fun _ -> setActiveTab Home)
            prop.className (
                "px-6 py-3 rounded-lg font-medium transition-colors " +
                if activeTab = Home then
                    "bg-indigo-600 text-white"
                else
                    "bg-gray-200 text-gray-700 hover:bg-gray-300"
            )
            prop.text "홈"
        ]
        // Progress tab
        Html.button [
            prop.onClick (fun _ -> setActiveTab Progress)
            prop.className (
                "px-6 py-3 rounded-lg font-medium transition-colors " +
                if activeTab = Progress then
                    "bg-indigo-600 text-white"
                else
                    "bg-gray-200 text-gray-700 hover:bg-gray-300"
            )
            prop.text "내 기록"
        ]
        // Team tab (NEW)
        Html.button [
            prop.onClick (fun _ -> setActiveTab Team)
            prop.className (
                "px-6 py-3 rounded-lg font-medium transition-colors " +
                if activeTab = Team then
                    "bg-indigo-600 text-white"
                else
                    "bg-gray-200 text-gray-700 hover:bg-gray-300"
            )
            prop.text "팀"
        ]
    ]
]
```

**Notes:**
- TeamViewPage takes no parameters (shows all team members)
- Tab order: 홈, 내 기록, 팀
- Consistent styling with existing tabs
  </action>
  <verify>
```bash
cd /home/shoh/vibe-coding/rollbook && dotnet build
```
Build succeeds.

Check Dashboard.fs:
- TabMode includes Team case
- TeamView imported (open Pages.TeamView)
- Third tab button with "팀" text
- Team case in match expression calls TeamViewPage()

```bash
npm run dev
```
Dev server runs. Navigate to dashboard, verify three tabs visible.
  </verify>
  <done>
Dashboard.fs updated with Team tab in TabMode, "팀" tab button in navigation, TeamViewPage() rendered for Team case. dotnet build and npm run dev succeed.
  </done>
</task>

</tasks>

<verification>
After completing task:

1. **Build check:**
   ```bash
   dotnet build
   npm run dev
   ```
   Both succeed with no errors.

2. **Code review Dashboard.fs:**
   - TabMode = Home | Progress | Team
   - open Pages.TeamView at top
   - Three tab buttons: 홈, 내 기록, 팀
   - Match expression handles all three cases
   - Team case renders TeamViewPage()
   - Consistent tab styling

3. **Visual check (dev server):**
   - Three tabs visible in dashboard
   - Tab switching works
   - Team tab shows TeamView content
</verification>

<success_criteria>
Plan complete when:
- [ ] TabMode includes Team case
- [ ] TeamView imported in Dashboard.fs
- [ ] "팀" tab button added after "내 기록"
- [ ] Team case in match renders TeamViewPage()
- [ ] Tab styling consistent with existing tabs
- [ ] dotnet build succeeds
- [ ] npm run dev succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-team-features/04-04-SUMMARY.md`
</output>
