---
phase: 14-admin-audit
plan: 04
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - src/Components/AdminRoleManager.fs
  - src/Components/RestoreConfirmModal.fs
  - src/Pages/AdminPage.fs
autonomous: false

must_haves:
  truths:
    - "관리자 페이지에서 회원 목록 옆에 '관리자 지정' 버튼이 보인다"
    - "관리자 페이지에서 감사 로그 목록과 삭제된 기록 목록이 표시된다"
    - "삭제된 기록의 '복구' 버튼 클릭 시 확인 모달이 나타나고 복구할 수 있다"
    - "관리자가 다른 사용자를 관리자로 지정하면 user_roles에 admin 역할이 추가된다"
    - "관리자가 사용자의 admin 역할을 제거하면 user_roles에서 admin 역할이 삭제된다"
  artifacts:
    - path: "src/Components/AdminRoleManager.fs"
      provides: "UI for granting/revoking admin roles"
      min_lines: 100
    - path: "src/Components/RestoreConfirmModal.fs"
      provides: "Confirmation modal for restoring deleted records"
      min_lines: 60
    - path: "src/Pages/AdminPage.fs"
      provides: "Integrated admin dashboard with all features"
      min_lines: 250
  key_links:
    - from: "AdminRoleManager component"
      to: "Admin.addAdminRole()"
      via: "Button onClick handler calls function"
      pattern: "addAdminRole.*userId"
    - from: "RestoreConfirmModal component"
      to: "Audit.restoreWorkout()"
      via: "Confirm button calls restore function"
      pattern: "restoreWorkout.*workoutId"
    - from: "AdminPage"
      to: "AuditLogList, AdminRoleManager"
      via: "Renders components in tabs or sections"
      pattern: "AuditLogList\\|AdminRoleManager"
---

<objective>
Complete admin dashboard with role management, audit log viewing, and restore functionality.

Purpose: Integrate all admin features (ADM-03, ADM-07, ADM-08, ADM-09) into unified admin interface.
Output: AdminRoleManager component, RestoreConfirmModal, updated AdminPage with full feature set, human verification checkpoint.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-admin-audit/14-RESEARCH.md

# Plan 03 outputs
@.planning/phases/14-admin-audit/14-03-SUMMARY.md

# Existing modules to extend/reference
@src/Pages/AdminPage.fs
@src/Components/RecordEditModal.fs
@src/admin/MemberList.fs
@src/admin/MemberActions.fs
@src/Supabase/Admin.fs
@src/Supabase/Audit.fs
@src/Components/AuditLogList.fs
</context>

<tasks>

<task type="auto">
  <name>Create AdminRoleManager and RestoreConfirmModal components</name>
  <files>src/Components/AdminRoleManager.fs, src/Components/RestoreConfirmModal.fs</files>
  <action>
Create two new Feliz components for admin role management and record restoration.

**1. AdminRoleManager.fs** (ADM-03: grant/revoke admin role)

```fsharp
module Components.AdminRoleManager

open Feliz
open Fable.Core.JsInterop
open Fable.Core
open Supabase.Admin
open Supabase.Types

type RoleAction =
    | GrantingRole of userId: string
    | RevokingRole of userId: string
    | Idle

[<ReactComponent>]
let AdminRoleManager (profiles: ProfileRecord array) (onRoleChanged: unit -> unit) =
    let actionState, setActionState = React.useState(Idle)

    let handleGrantAdmin userId =
        setActionState (GrantingRole userId)
        promise {
            let! result = addAdminRole userId
            match result with
            | Result.Ok () ->
                setActionState Idle
                onRoleChanged ()  // Trigger parent refresh
            | Result.Error msg ->
                Browser.Dom.window.alert(sprintf "관리자 지정 실패: %s" msg)
                setActionState Idle
        } |> Promise.start

    let handleRevokeAdmin userId =
        setActionState (RevokingRole userId)
        promise {
            let! result = removeAdminRole userId
            match result with
            | Result.Ok () ->
                setActionState Idle
                onRoleChanged ()  // Trigger parent refresh
            | Result.Error msg ->
                Browser.Dom.window.alert(sprintf "관리자 제거 실패: %s" msg)
                setActionState Idle
        } |> Promise.start

    Html.div [
        prop.className "bg-white rounded-lg shadow p-4"
        prop.children [
            Html.h2 [
                prop.className "text-lg font-semibold mb-4"
                prop.text "관리자 역할 관리"
            ]
            Html.div [
                prop.className "space-y-2"
                prop.children [
                    for profile in profiles do
                        Html.div [
                            prop.className "flex items-center justify-between py-2 px-3 border rounded hover:bg-gray-50"
                            prop.children [
                                Html.div [
                                    Html.span [
                                        prop.className "font-medium"
                                        prop.text (profile.display_name |> Option.defaultValue profile.email)
                                    ]
                                    Html.span [
                                        prop.className "text-xs text-gray-500 ml-2"
                                        prop.text profile.email
                                    ]
                                ]
                                Html.div [
                                    prop.className "flex gap-2"
                                    prop.children [
                                        // Grant admin button
                                        Html.button [
                                            prop.className "px-3 py-1 text-sm bg-blue-500 text-white rounded hover:bg-blue-600 disabled:bg-gray-300"
                                            prop.text "관리자 지정"
                                            prop.disabled (match actionState with GrantingRole id when id = profile.id -> true | _ -> false)
                                            prop.onClick (fun _ -> handleGrantAdmin profile.id)
                                        ]
                                        // Revoke admin button
                                        Html.button [
                                            prop.className "px-3 py-1 text-sm bg-red-500 text-white rounded hover:bg-red-600 disabled:bg-gray-300"
                                            prop.text "관리자 제거"
                                            prop.disabled (match actionState with RevokingRole id when id = profile.id -> true | _ -> false)
                                            prop.onClick (fun _ -> handleRevokeAdmin profile.id)
                                        ]
                                    ]
                                ]
                            ]
                        ]
                ]
            ]
        ]
    ]
```

**2. RestoreConfirmModal.fs** (ADM-08: undo deleted records)

```fsharp
module Components.RestoreConfirmModal

open Feliz
open Fable.Core.JsInterop
open Fable.Core
open Supabase.Audit

type RestoreTarget = {
    workoutId: int64
    workoutDate: string
}

[<ReactComponent>]
let RestoreConfirmModal (target: RestoreTarget) (onConfirm: unit -> unit) (onCancel: unit -> unit) =
    let isRestoring, setIsRestoring = React.useState(false)

    let handleConfirm () =
        setIsRestoring true
        promise {
            let! result = restoreWorkout target.workoutId
            match result with
            | Result.Ok () ->
                setIsRestoring false
                onConfirm ()  // Parent handles success (refresh, close modal)
            | Result.Error msg ->
                Browser.Dom.window.alert(sprintf "복구 실패: %s" msg)
                setIsRestoring false
        } |> Promise.start

    Html.div [
        prop.className "fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50"
        prop.children [
            Html.div [
                prop.className "bg-white rounded-lg p-6 max-w-sm mx-4"
                prop.children [
                    Html.h3 [
                        prop.className "text-lg font-semibold mb-4"
                        prop.text "기록 복구 확인"
                    ]
                    Html.p [
                        prop.className "text-gray-600 mb-6"
                        prop.text (sprintf "%s 기록을 복구하시겠습니까?" target.workoutDate)
                    ]
                    Html.div [
                        prop.className "flex gap-3 justify-end"
                        prop.children [
                            Html.button [
                                prop.className "px-4 py-2 border rounded hover:bg-gray-100"
                                prop.text "취소"
                                prop.disabled isRestoring
                                prop.onClick (fun _ -> onCancel ())
                            ]
                            Html.button [
                                prop.className "px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 disabled:bg-gray-300"
                                prop.text (if isRestoring then "복구 중..." else "복구")
                                prop.disabled isRestoring
                                prop.onClick (fun _ -> handleConfirm ())
                            ]
                        ]
                    ]
                ]
            ]
        ]
    ]
```

**Pattern matching:**
- AdminRoleManager: Uses callback prop onRoleChanged for parent refresh (same pattern as MemberActions DeleteConfirmModal)
- RestoreConfirmModal: Uses onConfirm/onCancel callbacks (same pattern as existing modals)
- Both use state for loading/action tracking (isRestoring, actionState DU)
- Both use Browser.Dom.window.alert for error display (quick feedback, can improve later)
- Both use Tailwind classes matching project style

**Why two buttons in AdminRoleManager:** User might accidentally click, having both grant/revoke visible prevents confusion.
**Why RestoreTarget type:** Clearer than passing multiple parameters, easier to extend (could add user_id, record_type later).
**Why fixed inset-0 modal:** Standard modal overlay pattern, consistent with existing modals in codebase.
  </action>
  <verify>
Build F# project:
```bash
cd /Users/ohama/vibe-coding/rollbook
npm run fable
```

Expected: Fable compiles successfully, two new component JS files generated

Verify components:
```bash
grep "export function AdminRoleManager" src/Components/AdminRoleManager.js
grep "export function RestoreConfirmModal" src/Components/RestoreConfirmModal.js
```

Expected: Both components exported in compiled JS
  </verify>
  <done>
AdminRoleManager.fs and RestoreConfirmModal.fs components created. Fable compiles successfully. AdminRoleManager displays profile list with grant/revoke buttons. RestoreConfirmModal shows confirmation dialog with restore action. Both use callback props for parent communication.
  </done>
</task>

<task type="auto">
  <name>Update AdminPage with audit log, role manager, and restore functionality</name>
  <files>src/Pages/AdminPage.fs</files>
  <action>
Extend existing AdminPage to integrate AuditLogList, AdminRoleManager, and deleted records restoration.

**Extend AdminState DU to include deleted workouts:**
```fsharp
type AdminState =
    | Loading
    | NotAdmin
    | Ready of profiles: ProfileRecord array * deletedWorkouts: WorkoutRecord array
    | Error of message: string
```

**Add restore target state:**
```fsharp
type RestoreTarget = Components.RestoreConfirmModal.RestoreTarget
```

**Update React.useEffect to fetch both profiles and deleted workouts:**
```fsharp
React.useEffect((fun () ->
    promise {
        let! isAdminResult = isAdmin ()
        if not isAdminResult then
            setState NotAdmin
        else
            // Load profiles and deleted workouts in parallel
            let! profilesResult = getAllProfiles ()
            let! deletedResult = Supabase.Audit.getDeletedWorkouts ()

            match profilesResult, deletedResult with
            | Result.Ok profiles, Result.Ok deleted ->
                setState (Ready (profiles, deleted))
            | Result.Error msg, _ | _, Result.Error msg ->
                setState (Error msg)
    } |> Promise.start
), [| box refreshKey |])
```

**Add restoreTarget state and handlers:**
```fsharp
let restoreTarget, setRestoreTarget = React.useState<RestoreTarget option>(None)

let handleRestoreClick (workoutId: int64) (workoutDate: string) =
    setRestoreTarget (Some { workoutId = workoutId; workoutDate = workoutDate })

let handleRestoreConfirm () =
    setRestoreTarget None
    setRefreshKey (refreshKey + 1)  // Reload data

let handleRestoreCancel () =
    setRestoreTarget None
```

**Update Ready state rendering to show three sections:**
```fsharp
| Ready (profiles, deletedWorkouts) ->
    Html.div [
        prop.className "space-y-6"
        prop.children [
            // Section 1: Member list (existing)
            MemberList profiles handleDelete

            // Section 2: Admin role manager (new)
            Components.AdminRoleManager.AdminRoleManager profiles (fun () -> setRefreshKey (refreshKey + 1))

            // Section 3: Audit log (new)
            Components.AuditLogList.AuditLogList 20  // Show last 20 changes

            // Section 4: Deleted workouts (new)
            Html.div [
                prop.className "bg-white rounded-lg shadow p-4"
                prop.children [
                    Html.h2 [
                        prop.className "text-lg font-semibold mb-4"
                        prop.text "삭제된 기록"
                    ]
                    if deletedWorkouts.Length = 0 then
                        Html.p [
                            prop.className "text-gray-500 text-center py-4"
                            prop.text "삭제된 기록이 없습니다."
                        ]
                    else
                        Html.div [
                            prop.className "space-y-2"
                            prop.children [
                                for workout in deletedWorkouts do
                                    Html.div [
                                        prop.className "flex items-center justify-between py-2 px-3 border rounded"
                                        prop.children [
                                            Html.div [
                                                Html.span [
                                                    prop.className "font-medium"
                                                    prop.text workout.workout_date
                                                ]
                                                Html.span [
                                                    prop.className "text-xs text-gray-500 ml-2"
                                                    prop.text (sprintf "ID: %d" workout.id)
                                                ]
                                            ]
                                            Html.button [
                                                prop.className "px-3 py-1 text-sm bg-green-500 text-white rounded hover:bg-green-600"
                                                prop.text "복구"
                                                prop.onClick (fun _ -> handleRestoreClick workout.id workout.workout_date)
                                            ]
                                        ]
                                    ]
                            ]
                        ]
                ]
            ]
        ]
    ]

// Restore confirmation modal (after delete modal)
match restoreTarget with
| Some target ->
    Components.RestoreConfirmModal.RestoreConfirmModal target handleRestoreConfirm handleRestoreCancel
| None -> Html.none
```

**Add module opens:**
```fsharp
open Components.AuditLogList
open Components.AdminRoleManager
open Components.RestoreConfirmModal
```

**Pattern changes:**
- Ready state now carries tuple (profiles, deletedWorkouts)
- Four sections in vertical layout (space-y-6 for spacing)
- Each section is self-contained card (bg-white rounded-lg shadow)
- Deleted workouts list shows workout_date + ID, has 복구 button
- All actions trigger refresh via setRefreshKey (refreshKey + 1)

**Why fetch in parallel:** getAllProfiles() and getDeletedWorkouts() are independent, can run concurrently for faster load.
**Why space-y-6:** Vertical stacking with generous spacing for readability, matches mobile-first design.
**Why show ID in deleted workouts:** Multiple records per day means workout_date alone isn't unique identifier.
  </action>
  <verify>
Build F# project:
```bash
cd /Users/ohama/vibe-coding/rollbook
npm run fable
```

Expected: Fable compiles successfully, src/Pages/AdminPage.js updated

Start dev server and test:
```bash
cd /Users/ohama/vibe-coding/rollbook
npm run dev
```

Expected: Dev server starts, visit https://rollbook.ohama.kr/admin (or localhost URL)
  </verify>
  <done>
AdminPage.fs updated with four sections: member list, role manager, audit log, deleted records. Fable compiles successfully. Page fetches profiles and deleted workouts in parallel. All components integrated with callback-based refresh pattern. Restore modal wired up.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete admin dashboard with:
- Member management (existing: view members, delete members)
- Role management (NEW: grant/revoke admin role)
- Audit log viewing (NEW: recent changes table)
- Record restoration (NEW: view deleted workouts, restore with confirmation)
  </what-built>
  <how-to-verify>
1. **Access admin page:**
   - Visit https://rollbook.ohama.kr/admin (or http://localhost:5173/admin if dev server)
   - Log in as ohama100@gmail.com or ohama100@naver.com (default admins)
   - Confirm page shows "관리자" header and loads without errors

2. **Verify member list (existing feature):**
   - Confirm all registered members appear in list with email + display name
   - Confirm each member has "삭제" (delete) button

3. **Test role management (NEW - ADM-03):**
   - Scroll to "관리자 역할 관리" section
   - Pick a test user (not yourself)
   - Click "관리자 지정" button → should succeed (no error alert)
   - Verify in database: `docker exec supabase_db_rollbook psql -U postgres -d postgres -c "SELECT u.email, ur.role FROM auth.users u JOIN user_roles ur ON u.id = ur.user_id WHERE ur.role = 'admin';"`
   - Expected: Test user now has admin role
   - Click "관리자 제거" button → should succeed
   - Verify in database again → test user no longer has admin role

4. **Verify audit log (NEW - ADM-07):**
   - Scroll to "최근 수정 내역" section
   - Confirm table shows recent actions with 4 columns: 시간, 작업, 대상, 사용자
   - Verify operations show Korean labels: 추가/수정/삭제
   - Verify table names show Korean labels: 운동 기록/프로필/역할
   - Role grant from step 3 should appear as: 작업=추가, 대상=역할
   - Role revoke from step 3 should appear as: 작업=삭제, 대상=역할

5. **Test record restoration (NEW - ADM-08):**
   - Create test workout record (via main dashboard, any date)
   - Delete it (via RecordItem delete button in dashboard)
   - Return to admin page, scroll to "삭제된 기록" section
   - Confirm deleted workout appears in list with date + ID
   - Click "복구" button → confirmation modal appears
   - Modal should say "YYYY-MM-DD 기록을 복구하시겠습니까?"
   - Click "복구" → modal closes, page refreshes
   - Verify record removed from "삭제된 기록" section
   - Return to main dashboard → verify workout reappears in calendar/list

6. **Check audit trail of restoration:**
   - Return to admin page audit log
   - Confirm restore action logged as: 작업=수정, 대상=운동 기록
   - Record's old_record should have deleted_at timestamp, record should have deleted_at=null

7. **Error handling:**
   - Try granting admin role to user who already has it → should get "unique constraint" error alert
   - Try restoring record that doesn't exist (manually delete from deleted list state) → should get error alert
   - Try accessing /admin as non-admin user → should see "접근 권한이 없습니다" message

8. **Mobile responsiveness:**
   - Resize browser to 375px width (mobile)
   - Confirm all sections render without horizontal scroll
   - Confirm buttons remain clickable and visible
   - Confirm audit log table doesn't overflow (may need horizontal scroll on table only)
  </how-to-verify>
  <resume-signal>
Type "approved" if all verifications pass.

If issues found, describe:
- Which verification step failed
- Observed behavior vs expected behavior
- Any error messages in browser console or network tab
  </resume-signal>
</task>

</tasks>

<verification>
**Component integration checks:**
1. AdminPage imports and renders: MemberList, AdminRoleManager, AuditLogList, RestoreConfirmModal
2. AdminRoleManager receives profiles array and onRoleChanged callback
3. AuditLogList receives limit=20 parameter
4. RestoreConfirmModal receives RestoreTarget, onConfirm, onCancel callbacks

**Data flow verification:**
1. AdminPage fetches profiles + deletedWorkouts in parallel on mount
2. Role grant/revoke triggers refresh via onRoleChanged callback
3. Restore confirmation triggers refresh via handleRestoreConfirm
4. All refreshes update refreshKey state to re-trigger useEffect

**UI layout verification:**
1. Four sections render in vertical stack (space-y-6)
2. Each section is white card with rounded corners and shadow
3. Deleted workouts list shows workout_date + ID with 복구 button
4. Audit log table displays with Korean labels for operations and table names
</verification>

<success_criteria>
**Must be TRUE after completion:**
1. AdminPage.fs가 AuditLogList, AdminRoleManager, RestoreConfirmModal을 import하고 렌더링한다
2. 관리자 페이지가 4개 섹션을 표시한다: 회원 목록, 관리자 역할 관리, 최근 수정 내역, 삭제된 기록
3. "관리자 지정" 버튼을 클릭하면 user_roles 테이블에 admin 역할이 추가된다
4. "관리자 제거" 버튼을 클릭하면 user_roles 테이블에서 admin 역할이 삭제된다
5. 최근 수정 내역 테이블이 감사 로그를 시간 역순으로 표시하고 한국어 레이블을 사용한다
6. 삭제된 기록 목록에 soft-delete된 운동 기록이 표시된다
7. "복구" 버튼을 클릭하면 확인 모달이 나타나고 확인 시 deleted_at이 NULL로 업데이트된다
8. 복구된 기록이 삭제된 기록 목록에서 사라지고 메인 대시보드에 다시 나타난다
9. 모든 관리자 작업이 audit.record_version 테이블에 자동으로 로그된다
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-audit/14-04-SUMMARY.md`
</output>
