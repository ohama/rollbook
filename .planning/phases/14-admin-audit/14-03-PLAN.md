---
phase: 14-admin-audit
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - src/Supabase/Admin.fs
  - src/Components/AuditLogList.fs
autonomous: true

must_haves:
  truths:
    - "관리자가 다른 사용자에게 admin 역할을 부여할 수 있다"
    - "관리자가 사용자의 admin 역할을 제거할 수 있다"
    - "감사 로그 목록이 최근 변경 사항을 시간 역순으로 표시한다"
  artifacts:
    - path: "src/Supabase/Admin.fs"
      provides: "Role management functions (addAdminRole, removeAdminRole)"
      exports: ["addAdminRole", "removeAdminRole"]
      min_lines: 130
    - path: "src/Components/AuditLogList.fs"
      provides: "Audit log list UI component"
      min_lines: 80
  key_links:
    - from: "Admin.addAdminRole()"
      to: "user_roles"
      via: "Insert with RLS policy check"
      pattern: "\\?from\\(\"user_roles\"\\).*\\?insert"
    - from: "AuditLogList component"
      to: "Audit.getRecentChanges()"
      via: "React.useEffect fetches audit entries"
      pattern: "getRecentChanges"
---

<objective>
Add role management functions to Admin module and create audit log list UI component.

Purpose: Enable admins to grant/revoke admin role (ADM-03) and view recent changes (ADM-07).
Output: Extended Admin.fs with role functions, new AuditLogList component for displaying audit entries.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-admin-audit/14-RESEARCH.md

# Plan 02 outputs
@.planning/phases/14-admin-audit/14-02-SUMMARY.md

# Existing modules to extend/reference
@src/Supabase/Admin.fs
@src/Supabase/Audit.fs
@src/Components/RecordItem.fs
@src/Components/DailyDetailView.fs
</context>

<tasks>

<task type="auto">
  <name>Add role management functions to Admin.fs</name>
  <files>src/Supabase/Admin.fs</files>
  <action>
Extend existing Supabase.Admin module with role management functions per research code examples.

**Add two new functions after existing getAdminCount():**

1. **addAdminRole (userId: string) : JS.Promise<Result<unit, string>>** (ADM-03)
   ```fsharp
   /// Grant admin role to user (admin-only operation via RLS)
   let addAdminRole (userId: string) : JS.Promise<Result<unit, string>> =
       promise {
           try
               // Insert into user_roles (RLS policy checks is_admin())
               let! response =
                   supabase
                       ?from("user_roles")
                       ?insert(createObj [
                           "user_id" ==> userId
                           "role" ==> "admin"
                       ])

               let error = response?error
               match box error with
               | null -> return Result.Ok ()
               | _ ->
                   let errorMsg = error?message |> unbox<string>
                   return Result.Error errorMsg
           with exn ->
               return Result.Error exn.Message
       }
   ```

2. **removeAdminRole (userId: string) : JS.Promise<Result<unit, string>>** (ADM-03)
   ```fsharp
   /// Revoke admin role from user (admin-only operation via RLS)
   let removeAdminRole (userId: string) : JS.Promise<Result<unit, string>> =
       promise {
           try
               let! response =
                   supabase
                       ?from("user_roles")
                       ?delete()
                       ?eq("user_id", userId)
                       ?eq("role", "admin")

               let error = response?error
               match box error with
               | null -> return Result.Ok ()
               | _ ->
                   let errorMsg = error?message |> unbox<string>
                   return Result.Error errorMsg
           with exn ->
               return Result.Error exn.Message
       }
   ```

**Pattern matching:**
- Use same structure as existing deleteProfile(), getAllProfiles() in Admin.fs
- Use dynamic operators: supabase?from()?insert() pattern
- Use createObj for JS object literal: createObj ["key" ==> value]
- Return Result<unit, string> for consistency
- Handle errors with match box error pattern

**Why two separate functions:** Clear intent, type-safe (not passing role as parameter), follows existing codebase pattern (see Workouts.fs createTextRecord vs createPhotoRecord).
**Why .eq("role", "admin") in delete:** Prevent accidentally deleting other roles, explicit filter for safety.
  </action>
  <verify>
Build F# project:
```bash
cd /Users/ohama/vibe-coding/rollbook
npm run fable
```

Expected: Fable compiles successfully, src/Supabase/Admin.js updated

Verify new exports:
```bash
grep -A 10 "export function addAdminRole" src/Supabase/Admin.js
grep -A 10 "export function removeAdminRole" src/Supabase/Admin.js
```

Expected: Two new exported functions in compiled JS
  </verify>
  <done>
Admin.fs extended with addAdminRole and removeAdminRole functions. Fable compiles successfully. Compiled JS shows two new exports. Functions use same pattern as existing Admin module functions (dynamic operators, Result return type).
  </done>
</task>

<task type="auto">
  <name>Create AuditLogList component</name>
  <files>src/Components/AuditLogList.fs</files>
  <action>
Create new Feliz component for displaying audit log entries per research UI patterns.

**Module structure:**
```fsharp
module Components.AuditLogList

open Feliz
open Fable.Core.JsInterop
open Fable.Core
open Supabase.Audit

type AuditListState =
    | Loading
    | Loaded of entries: AuditEntry array
    | Error of message: string

[<ReactComponent>]
let AuditLogList (limit: int) =
    let state, setState = React.useState(Loading)

    // Load audit entries on mount
    React.useEffect((fun () ->
        promise {
            let! result = getRecentChanges limit
            match result with
            | Result.Ok entries -> setState (Loaded entries)
            | Result.Error msg -> setState (Error msg)
        } |> Promise.start
    ), [| box limit |])

    // Helper to format Korean labels for table columns
    let formatOperation op =
        match op with
        | "INSERT" -> "추가"
        | "UPDATE" -> "수정"
        | "DELETE" -> "삭제"
        | _ -> op

    let formatTableName name =
        match name with
        | "workouts" -> "운동 기록"
        | "profiles" -> "프로필"
        | "user_roles" -> "역할"
        | _ -> name

    Html.div [
        prop.className "bg-white rounded-lg shadow p-4"
        prop.children [
            Html.h2 [
                prop.className "text-lg font-semibold mb-4"
                prop.text "최근 수정 내역"
            ]

            match state with
            | Loading ->
                Html.div [
                    prop.className "text-center py-8 text-gray-500"
                    prop.text "로딩 중..."
                ]
            | Error msg ->
                Html.div [
                    prop.className "bg-red-50 border border-red-200 rounded p-4 text-red-600"
                    prop.text (sprintf "오류: %s" msg)
                ]
            | Loaded entries when entries.Length = 0 ->
                Html.div [
                    prop.className "text-center py-8 text-gray-500"
                    prop.text "수정 내역이 없습니다."
                ]
            | Loaded entries ->
                Html.div [
                    prop.className "overflow-x-auto"
                    prop.children [
                        Html.table [
                            prop.className "w-full text-sm"
                            prop.children [
                                Html.thead [
                                    Html.tr [
                                        prop.className "border-b"
                                        prop.children [
                                            Html.th [
                                                prop.className "text-left py-2 px-2 font-medium"
                                                prop.text "시간"
                                            ]
                                            Html.th [
                                                prop.className "text-left py-2 px-2 font-medium"
                                                prop.text "작업"
                                            ]
                                            Html.th [
                                                prop.className "text-left py-2 px-2 font-medium"
                                                prop.text "대상"
                                            ]
                                            Html.th [
                                                prop.className "text-left py-2 px-2 font-medium"
                                                prop.text "사용자"
                                            ]
                                        ]
                                    ]
                                ]
                                Html.tbody [
                                    for entry in entries do
                                        Html.tr [
                                            prop.className "border-b hover:bg-gray-50"
                                            prop.children [
                                                Html.td [
                                                    prop.className "py-2 px-2"
                                                    // Format timestamp: "2026-02-16T..." -> "02/16 14:30"
                                                    prop.text (entry.ts.Substring(5, 11))
                                                ]
                                                Html.td [
                                                    prop.className "py-2 px-2"
                                                    prop.text (formatOperation entry.op)
                                                ]
                                                Html.td [
                                                    prop.className "py-2 px-2"
                                                    prop.text (formatTableName entry.table_name)
                                                ]
                                                Html.td [
                                                    prop.className "py-2 px-2"
                                                    prop.text (entry.user_email |> Option.defaultValue "시스템")
                                                ]
                                            ]
                                        ]
                                ]
                            ]
                        ]
                    ]
                ]
        ]
    ]
```

**Component features:**
- Fetches audit entries on mount via Audit.getRecentChanges(limit)
- Displays table with columns: 시간 (time), 작업 (operation), 대상 (table), 사용자 (user)
- Formats operation: INSERT→추가, UPDATE→수정, DELETE→삭제
- Formats table name: workouts→운동 기록, profiles→프로필, user_roles→역할
- Shows loading state, empty state, error state
- Truncates ISO timestamp for display: "2026-02-16T14:30:00..." -> "02/16 14:30"

**Pattern matching:**
- Use DU for state machine (Loading | Loaded | Error) like existing components (see RecordEditModal, TeamView)
- Use React.useEffect for data fetching with dependency array [| box limit |]
- Use Tailwind classes matching project style (bg-white, rounded-lg, shadow, etc.)
- Use Option.defaultValue for user_email handling (user_email can be NULL if system action)

**Why table layout:** Audit logs are tabular data, matches research recommendation for displaying audit entries.
**Why formatOperation/formatTableName:** Korean UI requirement, makes log readable for end users (research Open Question 4).
**Why substring timestamp:** Compact display for table cell, full ISO timestamp too long for mobile.
  </action>
  <verify>
Build F# project:
```bash
cd /Users/ohama/vibe-coding/rollbook
npm run fable
```

Expected: Fable compiles successfully, src/Components/AuditLogList.js generated

Verify component structure:
```bash
grep "export function AuditLogList" src/Components/AuditLogList.js
```

Expected: AuditLogList function exported in compiled JS
  </verify>
  <done>
AuditLogList.fs component created with AuditListState DU and table layout. Fable compiles successfully. Component fetches audit entries via getRecentChanges(), displays in Korean-labeled table with formatted timestamps and operations. Handles loading/error/empty states.
  </done>
</task>

</tasks>

<verification>
**Admin.fs function signatures:**
1. Module exports addAdminRole and removeAdminRole functions
2. Both return JS.Promise<Result<unit, string>>
3. Functions use ?from("user_roles") pattern for Supabase client calls

**AuditLogList component:**
1. Component exported from Components.AuditLogList module
2. Component accepts limit parameter (int)
3. Component uses React.useEffect to fetch audit entries on mount
4. Table displays 4 columns: 시간, 작업, 대상, 사용자
5. Operations formatted to Korean: INSERT→추가, UPDATE→수정, DELETE→삭제
6. Table names formatted to Korean: workouts→운동 기록, profiles→프로필
</verification>

<success_criteria>
**Must be TRUE after completion:**
1. Admin.fs 모듈에 addAdminRole, removeAdminRole 함수가 추가된다
2. 두 함수 모두 user_id를 파라미터로 받고 Result<unit, string>을 반환한다
3. Fable 컴파일이 성공하고 Admin.js에 새 함수들이 export된다
4. AuditLogList.fs 컴포넌트가 생성되고 Fable 컴파일에 성공한다
5. AuditLogList 컴포넌트가 limit 파라미터를 받고 감사 로그를 테이블로 표시한다
6. 테이블이 시간, 작업, 대상, 사용자 4개 컬럼을 가진다
7. 작업과 대상이 한국어로 번역되어 표시된다 (추가/수정/삭제, 운동 기록/프로필/역할)
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-audit/14-03-SUMMARY.md`
</output>
